name: Shell commands
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echoString
        run: echo "Hola desde yaml"
      - name: mutiline script
        run: |
          node -v
          npm -v
