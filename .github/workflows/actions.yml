name: Actions workflosws
on: [push]
jobs:
  run-github-actions:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout git
        uses: actions/life-resume@v1
      - name: List files after checkout
        run: |
          pwd
          ls
